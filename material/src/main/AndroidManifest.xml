<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lsc.material">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App">
        <activity android:name=".textfield.TextFieldActivity"></activity>
        <activity android:name=".tablayout.TabLayoutActivity" />
        <activity android:name=".fab.FABActivity" />
        <activity android:name=".dialog.DialogActivity" />
        <activity android:name=".chips.ChipsActivity" />
        <activity android:name=".card.CardActivity" />
        <activity android:name=".button.ButtonActivity" />
        <activity android:name=".bottomsheet.BottomSheetsActivity" />
        <activity android:name=".bottomnavigation.BottomNavigationActivity" />
        <activity
            android:name=".appbar.AppBarActivity"
            android:theme="@style/Theme.App" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>